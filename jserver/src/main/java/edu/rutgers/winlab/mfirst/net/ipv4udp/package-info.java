/**
 * Provides networking services for GNRS over IPv4/UDP.
 * 
 * <p>A network implementation for the GNRS server over an
 * IP (version 4) network using UDP as the transport layer.  Uses
 * configuration files to provide IP network prefixes (similar to BGP
 * announcements) and address bindings for configured Autonomous System
 * GNRS servers.</p>
 */
package edu.rutgers.winlab.mfirst.net.ipv4udp;